name: Test Deployment 🧪

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test-deploy:
    name: Test Deployment 🧪
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository 📦
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Nodejs 🧰
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install Dependencies 📚
        run: npm ci

      - name: Test Build Website 🛠️
        run: npm run build
